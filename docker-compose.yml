services:

  # ── Nginx リバースプロキシ ─────────────────────────────────────────────────
  nginx:
    build: ./nginx  # nginx.conf を焼き込んだカスタムイメージをビルド（本番と統一）
    ports:
      - "80:80"
    depends_on:
      web:
        condition: service_healthy
    restart: unless-stopped

  # ── Flask + Gunicorn (DateCalc) ───────────────────────────────────────────
  web:
    # IMAGE_TAG は GitHub Actions が SHA タグを .env に書き出して渡す
    # ローカル起動時のフォールバックとして latest を使用
    image: ghcr.io/horikawatakato/datecalc:${IMAGE_TAG:-latest}
    environment:
      - TZ=Asia/Tokyo          # コンテナのタイムゾーンを日本時間に設定
      - WEB_CONCURRENCY=2      # Gunicorn ワーカー数（コンテナ資源に合わせ調整）
    expose:
      - "8000"           # Nginx からのみアクセス可。外部には公開しない。
    healthcheck:
      # /api/today は軽量なので healthcheck に使う
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/today')"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    restart: unless-stopped
